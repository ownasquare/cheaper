# GitHub Sponsors profile — paste-ready copy for `cheaperapp`

Modeled on the `iamtoruk` Sponsors layout, rewritten for Cheaper. Paste these into
the GitHub Sponsors dashboard when you enroll the `cheaperapp` account
(**Settings → Sponsors**, or https://github.com/sponsors/dashboard). GitHub requires
the account owner to do identity verification, connect a bank/Stripe payout, and
submit tax info — that part can't be scripted.

## Short bio / headline

> Cheaper is a free, MIT-licensed, local-first **model router** for AI coding tools.
> It triages every request and runs the cheapest capable model — so you stop paying
> top-tier prices for the easy work — with a monitor that proves the savings.
> An [Own a Square](https://ownasquare.com) project. Sponsorship keeps it free, adds
> more supported tools, and keeps the savings estimates honest.

## Sponsorship goal

> **50 monthly sponsors** to fund full-time work on Cheaper — more supported tools,
> real-number savings, and no paywalls, ever.

## Monthly tiers

### $5 / month — "Sponsor badge"
- Sponsor badge on your GitHub profile
- Helps keep Cheaper free and MIT for everyone

### $25 / month — "Name in the README"
- Your name featured in the Cheaper README
- Priority consideration on feature requests
- Access to the sponsors channel on Discord

### $100 / month — "Logo & highlight"
- Your logo + a special mention in the README and on cheaper.app
- Priority bug handling
- Early access to new supported tools and features before public release

## One-time tier

### "Buy the routers a coffee" — any amount
- One-off support with a thank-you in the next release notes

## After enrolling — repoint the links (if needed)

The repo button (`.github/FUNDING.yml`) and the site/README already point to
`github.com/sponsors/cheaperapp`. If you decide to host the profile under a different
account (e.g. `ownasquare` or `iamtoruk`), update these to match:

- `cheaper-app/.github/FUNDING.yml` → `github: [<account>]`
- `cheaper-app/README.md` → the three `github.com/sponsors/cheaperapp` links
- `../cheaper-web/web/*.html` footers → the `Sponsor` links (`github.com/sponsors/cheaperapp`)
