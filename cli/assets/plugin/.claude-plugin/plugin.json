{
  "name": "adaptive-model-router",
  "version": "0.3.0",
  "description": "Always-on adaptive model routing: classify each request in-session and run it on the cheapest sufficient tier, delegating to a stronger-model subagent when the request needs one, and size spawned subtask agents by complexity. Bundles the routing skill plus a hook that keeps the policy active every turn.",
  "author": {
    "name": "Beladed"
  },
  "keywords": ["model-routing", "cost-optimization", "triage", "subagents", "adaptive"]
}
