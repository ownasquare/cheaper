{
  "name": "cheaper-app-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Test harness for the Cheaper.app repo. NOT published — the npm package is cli/package.json, whose `files` list ships only bin/, src/ and assets/. Playwright lives here so the shipped package keeps its empty dependency tree.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "test:cli": "npm --prefix cli test",
    "test:gateway": "python3 -m pytest gateway/tests -q",
    "test:all": "npm run test:cli && npm run test:gateway && npm run test:e2e"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.60.0",
    "axe-core": "^4.13.0"
  }
}
